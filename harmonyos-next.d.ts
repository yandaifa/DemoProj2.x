declare namespace cc {
    namespace sys {
        /** HarmonyOS NEXT 平台常量 */
        const HARMONYOS_NEXT: number;   // 12
        /** 等价宏，推荐用做 os 判断 */
        const OS_OPENHARMONY: string;   // "OpenHarmony"
    }
}